# AGENTS.md

Skipper is specified in `spec/` and built from it. Whatever coding agent you are
(Claude Code, Codex, Cursor, OpenCode, gstack-driven, or other), read `CLAUDE.md`
for build procedure and `spec/` in numeric order for the authoritative
specification. The spec is the contract; when in doubt, change the spec first,
then the code.
